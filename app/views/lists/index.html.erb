<ol>
<% @lists.each do |list| %>
<li>
<%= link_to "#{list.name}", "lists/#{list.id}" %>
</li>
<% end %>
</ol>
<button type="button" class="btn btn-primary">New List<%= link_to "New list", "lists/new" %></button>
